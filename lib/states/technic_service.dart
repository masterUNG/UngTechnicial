import 'package:flutter/material.dart';

class TeachnicService extends StatelessWidget {
  const TeachnicService({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}